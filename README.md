# Yandex Disk Telegram Bot

Telegram бот для загрузки файлов на Яндекс Диск и получения публичных ссылок.

## Возможности

- 📤 Загрузка любых типов файлов (документы, фото, видео, архивы)
- 🔗 Автоматическое создание публичных ссылок
- 📊 Прогресс-бар при загрузке больших файлов
- 📋 Просмотр истории загруженных файлов
- 🗑️ Удаление файлов через бота
- 🔒 Безопасное хранение OAuth токенов (шифрование Fernet)
- 📁 Создание пользовательских папок для организации файлов
- ⚡ Поддержка файлов до 2 GB (Telegram Premium)

## Архитектура

Модульная архитектура для легкого расширения:

```
bot/
├── core/              # Ядро системы
│   ├── bot.py        # Инициализация aiogram
│   ├── database.py   # SQLAlchemy async
│   ├── crypto.py     # Шифрование токенов
│   └── middleware.py # Auth, Logging, RateLimit
├── modules/          # Независимые модули
│   ├── common/       # Базовые команды
│   └── yandex/       # Модуль Яндекс Диска
├── config.py         # Конфигурация
└── main.py          # Точка входа
```

## Требования

- Python 3.11+
- Telegram Bot Token
- Yandex OAuth Token (для каждого пользователя)

## Установка и запуск

### Вариант 1: Docker (рекомендуется)

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd YADISKTGBOT
```

2. **Создайте .env файл:**
```bash
cp .env.example .env
```

3. **Настройте переменные окружения в .env:**

```env
# Обязательно:
BOT_TOKEN=your_telegram_bot_token
ENCRYPTION_KEY=your_fernet_key

# Опционально:
YANDEX_CLIENT_ID=your_yandex_client_id
```

**Как получить BOT_TOKEN:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**Как сгенерировать ENCRYPTION_KEY:**
```python
from cryptography.fernet import Fernet
print(Fernet.generate_key().decode())
```

**Как получить YANDEX_CLIENT_ID (опционально):**
1. Перейдите на [Яндекс OAuth](https://oauth.yandex.ru/client/new)
2. Зарегистрируйте новое приложение
3. Скопируйте Client ID

4. **Запустите бота:**
```bash
docker-compose up -d
```

5. **Просмотр логов:**
```bash
docker-compose logs -f bot
```

6. **Остановка бота:**
```bash
docker-compose down
```

### Вариант 2: Локальный запуск

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd YADISKTGBOT
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте .env файл:**
```bash
cp .env.example .env
```

Настройте переменные окружения (см. выше).

5. **Запустите бота:**
```bash
python main.py
```

## Использование

### Для пользователей бота

1. **Старт бота:**
```
/start
```

2. **Настройка OAuth токена:**
```
/token
```

Получите OAuth токен Яндекс Диска:
1. Перейдите по ссылке (бот покажет её)
2. Разрешите доступ к Яндекс Диску
3. Скопируйте токен из URL (после `#access_token=`)
4. Отправьте токен боту

3. **Создание папки:**
После настройки токена бот предложит создать папку для файлов или использовать корневую директорию.

4. **Загрузка файлов:**
Просто отправьте любой файл боту - он загрузит его на Яндекс Диск и пришлёт публичную ссылку.

5. **Управление файлами:**
```
/list   - показать загруженные файлы
```
Удалить файл можно через кнопку в списке.

### Команды бота

- `/start` - приветствие и инструкции
- `/help` - подробная справка
- `/token` - настройка OAuth токена
- `/list` - список загруженных файлов

## Безопасность

- ✅ OAuth токены шифруются перед сохранением (Fernet/AES)
- ✅ Сообщения с токенами автоматически удаляются
- ✅ Временные файлы удаляются после загрузки
- ✅ Rate limiting (5 запросов/сек на пользователя)
- ⚠️ Храните файл `bot.db` в безопасности
- ⚠️ Не публикуйте `.env` файл

## Разработка

### Структура проекта

```
YADISKTGBOT/
├── core/                    # Ядро системы
│   ├── __init__.py
│   ├── bot.py              # BotCore класс
│   ├── crypto.py           # TokenEncryption
│   ├── database.py         # SQLAlchemy async
│   └── middleware.py       # Middleware
├── modules/                 # Модули
│   ├── common/             # Базовые команды
│   │   └── handlers.py
│   └── yandex/             # Яндекс Диск
│       ├── __init__.py
│       ├── handlers.py     # FSM, команды
│       ├── keyboards.py    # Inline клавиатуры
│       ├── models.py       # SQLAlchemy модели
│       ├── service.py      # YandexDiskAPI
│       └── utils.py        # Вспомогательные функции
├── docs/                   # Документация
│   └── plans/
├── config.py               # Конфигурация
├── main.py                # Точка входа
├── requirements.txt        # Зависимости
├── Dockerfile
├── docker-compose.yml
└── .env.example

### Добавление новых модулей

Создайте новый модуль в `modules/`:

```python
# modules/new_module/__init__.py
def setup(dp):
    """Setup function called by BotCore."""
    from .handlers import router
    dp.include_router(router)
    print("✅ New module loaded")
```

Добавьте в `ENABLED_MODULES` в `.env`:
```
ENABLED_MODULES=modules.common,modules.yandex,modules.new_module
```

## Технологии

- **aiogram 3** - асинхронный Telegram framework
- **SQLAlchemy** - ORM для работы с БД
- **aiohttp** - асинхронный HTTP клиент для Yandex API
- **Cryptography** - шифрование OAuth токенов
- **SQLite** - база данных (можно заменить на PostgreSQL)

## Лицензия

MIT

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs -f bot`
2. Убедитесь что BOT_TOKEN и ENCRYPTION_KEY заданы
3. Проверьте валидность OAuth токена Яндекс Диска
4. Убедитесь что на Яндекс Диске есть свободное место

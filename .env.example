# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
# 1. Open https://t.me/BotFather
# 2. Send /newbot and follow instructions
# 3. Copy the token and paste it below
BOT_TOKEN=your_telegram_bot_token_here

# Encryption Key for storing OAuth tokens securely
# Generate a new key by running in Python:
#   from cryptography.fernet import Fernet
#   print(Fernet.generate_key().decode())
# Then paste the result below
ENCRYPTION_KEY=your_fernet_encryption_key_here

# Yandex OAuth Client ID (optional)
# Required if you want to show users direct OAuth URL
# Register app at https://oauth.yandex.ru/client/new
# After registration, paste the Client ID below
# Make sure to add scopes: cloud_api:disk.read, cloud_api:disk.write
YANDEX_CLIENT_ID=your_yandex_client_id_here

# Database (optional, defaults to sqlite:///bot.db)
# For Docker: use a directory path to avoid file mount issues
# DATABASE_URL=sqlite+aiosqlite:///app/data/bot.db

# File Upload Limits (optional)
# MAX_FILE_SIZE=2147483648  # 2 GB in bytes

# Rate Limiting (optional)
# RATE_LIMIT=5  # requests per second per user

# Temporary Files (optional)
# TEMP_DIR=/tmp/telegram_bot_files
# CLEANUP_INTERVAL_HOURS=24

# Logging (optional)
# LOG_LEVEL=INFO

# Enabled Modules (optional)
# ENABLED_MODULES=modules.common,modules.yandex,modules.github

# GitHub Webhooks (optional)
# Set WEBHOOK_HOST to your VPS public IP (or domain) to enable GitHub webhook notifications
# Bot will start an HTTP server on WEBHOOK_PORT to receive webhook events
# WEBHOOK_HOST=123.45.67.89
# WEBHOOK_PORT=8443

# Local Bot API Server (for files > 20 MB, up to 2 GB)
# Enable this to bypass Telegram's 20 MB file size limit
# You'll need API credentials from https://my.telegram.org
USE_LOCAL_API=false
LOCAL_API_SERVER=http://telegram-bot-api:8081
TELEGRAM_API_ID=your_api_id_from_my_telegram_org
TELEGRAM_API_HASH=your_api_hash_from_my_telegram_org
